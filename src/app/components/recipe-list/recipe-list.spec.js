describe('recipe-list', () => {
  
  let $filter;
  let MyService;
  beforeEach(() => {
    angular.mock.module('app');
    angular.mock.inject(() => {

    });
  });

  it("should ...", () => {

  });


});
